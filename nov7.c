#include<stdio.h>
#include<string.h>

int main()
{
	int d,i;
	char len1[100001],len2[100001],ln[67];
	scanf("%d",&d);
	
	for(i=1;i<=d;i++)
	{	
		gets(len1);
	}
	gets(len2);
	puts(len2);
	
	return 0;
}